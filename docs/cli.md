## 🚀 CLI（コマンドライン）仕様

当プロジェクトは、以下の 4 ステップで構成されます。  
各ステップは race_id / distance / course を引数で受け取り、  
ファイル名はすべて自動生成されます。

---

# 1. 🐴 出走表＋過去5走の取得（クロール）

    python -m crawl.crawl_shutuba --race_id 202505050812

### 自動生成ファイル

    assets/race_202505050812_raw.csv

---

# 2. 🧮 過去走スコア算出（speed / closing / lead）

    python -m scoring.score_past5 --race_id 202505050812 --distance 1600

### 自動生成ファイル

    assets/race_202505050812_1600m_scores.csv

---

# 3. 🏟 コース適性スコア付与

    python -m course.course_score --race_id 202505050812 --distance 1600 --course 東京

### 自動生成ファイル

    assets/race_202505050812_1600m_東京_course.csv

---

# 4. 📊 ランキング表示（予想結果）

    python -m predict.simple_rank --race_id 202505050812 --distance 1600 --course 東京

### 出力内容
- ターミナルに <コース>適性スコア順ランキング表示  
- ファイル出力はなし

---

## 📂 自動生成ファイル命名規則

| ステージ | 生成ファイル形式 |
|---------|------------------------------|
| crawl | race_<race_id>_raw.csv |
| scoring | race_<race_id>_<distance>m_scores.csv |
| course | race_<race_id>_<distance>m_<course>_course.csv |
| predict | なし（表示のみ） |

※ すべて assets/ に保存されます。

---

## 🧭 一連の実行フローまとめ

    python -m crawl.crawl_shutuba --race_id 202505050812
    python -m scoring.score_past5 --race_id 202505050812 --distance 1600
    python -m course.course_score --race_id 202505050812 --distance 1600 --course 東京
    python -m predict.simple_rank --race_id 202505050812 --distance 1600 --course 東京

---

## 📝 CLI 引数仕様

| 引数名 | 例 | 説明 |
|--------|-----------------|-----------------------------|
| --race_id | 202505050812 | ネット競馬のレースID（必須） |
| --distance | 1600 | 予想レース距離（m） |
| --course | 東京 | 開催コース名（東京 / 中山 / 京都 / 阪神） |

---

## ⚠️ 注意事項
- コース名は「東京 / 中山 / 京都 / 阪神」から指定  
- ファイル名は race_id / distance / course から自動生成  
- Chrome + Selenium を使用するためネット環境が必要
